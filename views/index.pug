extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      img.logo(src='./static/images/jlh-logo.svg')
      h1 The Portfolio of Awesomeness

      p.lead.text-light
        | These four projects represent the best of my experience, both as a Treehouse student and creating my own projects. Have a look at these amazing projects and your life will never be the same. Also... hire me!
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- loop creates project thumbnails and links
      each project in projects
        .cell
          a(href='project/'+project.id)
            img.thumbnail(src=`${project.image_urls[0]}`)
            h5 #{project.project_name}

